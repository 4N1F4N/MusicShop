<ul>
  <% @orders.each do |order| %>
    <li>
      <a>Статус: <%= order.order_status.last.title %></a>
      <a>Id: <%= order.id %></a>
      <a href="<%= admin_order_path(order.id) %>">Подробная информация</a>
      <a href="<%= admin_order_cancelled_path(order.id) %>">Отменить заказ</a>
    </li>
  <% end %>
</ul>